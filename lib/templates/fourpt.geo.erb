0                  / output index ( 0 - no echo, 1 - echo ).

<% 
@channels.each do |c|
%>
<%= c.number %>                           / channel number.
<%= c.resistance %>                       / flow resistance coefficient.
Upstream_<%= c.number %>                  / upstream cx ID.
0.0 <%= sprintf "%.1f", c.upstream.width %> <%= sprintf "%.1f", c.upstream.elevation %>           / downstream ref. distance, width, btmelev.
Downstream_<%= c.number %>                / downstream cx ID.
<%= sprintf "%.1f", c.length %> <%= sprintf "%.1f", c.downstream.width %> <%= c.downstream.elevation %>              / downstream ref. distance, width, btmelev.
<% end %>
